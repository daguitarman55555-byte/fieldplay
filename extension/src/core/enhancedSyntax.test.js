import{describe,expect,it}from'vitest';import{parseEnhancedSyntax}from'./enhancedSyntax.js';import{normalizeDesmosLatex,parseDesmosSlopeField,parseDesmosVectorField}from'./desmosLatex.js';
describe('enhanced Desmos syntax',()=>{
  it('parses vectors and vector fields',()=>{expect(parseEnhancedSyntax('vec a = <<2,3>>')).toMatchObject({kind:'vector',name:'a',components:['2','3']});expect(parseEnhancedSyntax('vec F(x,y) = <<-y,x>>')).toMatchObject({kind:'vector-field',variables:['x','y']});expect(parseEnhancedSyntax('vec F = <<-y,x')).toMatchObject({kind:'vector',components:['-y','x']});});
  it('parses calculus notation',()=>{expect(parseEnhancedSyntax('partial x y f(x,y)')).toMatchObject({kind:'partial',variables:['x','y']});expect(parseEnhancedSyntax('d n x f(x)')).toMatchObject({kind:'derivative',order:'n'});expect(parseEnhancedSyntax('grad f(x,y)')).toMatchObject({kind:'grad'});expect(parseEnhancedSyntax("y'=x-y")).toMatchObject({kind:'slope-field'});});
  it('reads native and enhanced Desmos functions as fields',()=>{expect(parseDesmosVectorField('F\\left(x,y\\right)=\\left(-y,x\\right)')).toMatchObject({name:'F',x:'-y',y:'x'});expect(parseDesmosVectorField('F\\left(x,y\\right)=\\left\\langle-y,x\\right\\rangle')).toMatchObject({name:'F',x:'-y',y:'x'});expect(parseDesmosVectorField('\\vec{F}=<<-y,x>>')).toMatchObject({name:'F',x:'-y',y:'x'});expect(parseDesmosVectorField('\\vec{F}=<<-y,x')).toMatchObject({name:'F',x:'-y',y:'x'});expect(parseDesmosVectorField('f(x)=x')).toBeNull();});
  it('reads native slope fields',()=>{expect(parseDesmosSlopeField("y^{\\prime}=x-y")).toMatchObject({kind:'slope-field',x:'1',y:'x-y'});expect(parseDesmosSlopeField('y=x')).toBeNull();});
  it('normalizes common Desmos latex',()=>expect(normalizeDesmosLatex('\\frac{x}{2}+\\sin\\left(y\\right)')).toBe('((x)/(2))+sin(y)'));
});
